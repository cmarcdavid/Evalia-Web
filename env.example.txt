# ══════════════════════════════════════════════════
# EVALIA — Environment Variables
# ══════════════════════════════════════════════════
# Copy this file to .env.local for local development
# Add these variables in Vercel Dashboard → Project → Settings → Environment Variables
# NEVER commit .env.local or real values to Git
# ══════════════════════════════════════════════════

# ── CLOUDINARY ──────────────────────────────────
# Cloud name is PUBLIC — safe to use in frontend code
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dueczmagh

# API Key & Secret are PRIVATE — server-side only, never in frontend
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# Upload preset (unsigned) — create in Cloudinary Dashboard → Settings → Upload Presets
# Set as "Unsigned" and restrict to folder: evalia/
CLOUDINARY_UPLOAD_PRESET=evalia_unsigned

# ── FORMSPREE ────────────────────────────────────
FORMSPREE_BOOKING_ID=your_booking_form_id
FORMSPREE_CONTACT_ID=your_contact_form_id

# ── STRIPE (when ready for live payments) ────────
STRIPE_PUBLISHABLE_KEY=pk_live_your_key_here
STRIPE_SECRET_KEY=sk_live_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# ── EMAIL ────────────────────────────────────────
SENDGRID_API_KEY=SG.your_key_here
FROM_EMAIL=hello@evalia.co
